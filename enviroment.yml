name: robot
channels:
  - defaults
dependencies:
  - conda
  - pip
  - python=3.7
  - conda-env
  - conda-build
  - conda-verify
  - scipy
  - pip:
    - opencv-python
    - git+https://github.com/ideasman42/isect_segments-bentley_ottmann
